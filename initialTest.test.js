const productController = require('./controllers/productController');
const productModel = require('./models/productModel');

describe('teste inicial para verificar se tudo ok', () => {
  expect(2+2).toBe(4);
});

